using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using Trainworks.BuildersV2;
using Trainworks.ConstantsV2;
using UnityEngine;


namespace CustomEffectsPathogens
    //deleted from the actual mod as it was unused
{
    /*internal class CustomCardEffectReserveAndPurge : CardEffectBase
    {
        public override IEnumerator ApplyEffect(CardManager.DiscardCardParams discardCardParams, CardManager cardManager, RelicManager relicManager, CombatManager combatManager, RoomManager roomManager, SaveManager saveManager)
        {
            if (discardCardParams.handDiscarded)
            { yield return cardManager.PurgeCard(discardCardParams.discardCard); }
        }
        

        
    }*/
}
